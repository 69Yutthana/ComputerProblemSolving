void main() {
    IO.println("Enter score 1: ");
    double S1 = Double.parseDouble(IO.readln());
    IO.println("Enter score 2: ");
    double S2 = Double.parseDouble(IO.readln());
    IO.println("Enter score 3: ");
    double S3 = Double.parseDouble(IO.readln());
    IO.println("Enter bonus : ");
    double Bonus = Double.parseDouble(IO.readln());
    double TS = S1 + S2 + S3;
    double TWB = TS + Bonus;
    double averageOfTwb = TS /3
IO.println("Total score = " + TS);
    IO.println("Total with bonus = " + TWB);
    IO.println("Average of totalWithBonus(%.0f) = %2f " poins ) ;
}